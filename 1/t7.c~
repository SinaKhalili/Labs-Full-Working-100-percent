#include<stdio.h>
int main() {
  int counter = 0;
  int input = 0;
  int scanTester = scanf("%d",&input);
  while (scanTester != EOF){
    for(counter; counter<input ; counter++ ){
      printf("#");
    }
    printf("\n");
    counter = 0;
    scanTester = scanf("%d",&input);
  }
}
